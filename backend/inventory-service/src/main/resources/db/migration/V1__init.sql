-- inventory-service 스키마 시작점. 재고원장(append-only)·재고/로케이션 투영 테이블을 여기 정의한다.
-- 예: stock_movement(원장, append-only), stock_on_hand(투영), stock_hold(홀드/예약).
-- 지금은 골격만 — 실제 DDL 은 도메인 설계 확정 후 V2__*.sql 부터 이어서 추가한다.
SELECT 1;
